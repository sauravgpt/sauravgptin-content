---
title: "Two Pointers & Sliding Window"
secondaryTitle: "Two Pointers"
order: 1
description: "The linear-scan patterns that turn O(n²) brute force into O(n) — converging pointers and dynamic windows."
---

## Concept Overview

Two pointers and sliding window exploit sorted order or contiguity to examine each element a constant number of times instead of re-scanning.

> Content coming soon. This lesson will cover:

- Converging pointers on sorted input
- Fixed-size vs. variable-size sliding windows
- When each pattern applies (and when it doesn't)
- Classic problems: container with most water, longest substring without repeats

```callout
{ "type": "info", "content": "This lesson is a stub. Full content is being written." }
```
